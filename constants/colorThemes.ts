/**
 * Color Theme Examples
 *
 * To change the primary color of your app, simply update these values in:
 * 1. constants/theme.ts (primary and primaryLight)
 * 2. tailwind.config.js (primary and primary-light)
 *
 * Here are some pre-configured color schemes you can use:
 */

// Current: Blue Theme
export const blueTheme = {
  primary: "#3b82f6",
  primaryLight: "#eff6ff",
};

// Alternative: Purple Theme (like in your reference image)
export const purpleTheme = {
  primary: "#8b5cf6",
  primaryLight: "#f3f4f6",
};

// Alternative: Green Theme
export const greenTheme = {
  primary: "#10b981",
  primaryLight: "#ecfdf5",
};

// Alternative: Orange Theme
export const orangeTheme = {
  primary: "#f59e0b",
  primaryLight: "#fffbeb",
};

// Alternative: Pink Theme
export const pinkTheme = {
  primary: "#ec4899",
  primaryLight: "#fdf2f8",
};

// Alternative: Red Theme
export const redTheme = {
  primary: "#ef4444",
  primaryLight: "#fef2f2",
};

/**
 * To apply a theme:
 * 1. Choose a theme from above
 * 2. Copy the primary and primaryLight values
 * 3. Replace them in constants/theme.ts
 * 4. Replace the corresponding values in tailwind.config.js
 * 5. Restart your development server
 */
